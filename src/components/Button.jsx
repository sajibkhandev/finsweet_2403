import React from 'react'

const Button = ({text}) => {
  return (
    <button className='text-[#fff] font-pop  bg-transparent py-4 px-12 rounded-[40px] border-2 border-[#474974] hover:bg-[#FCD980] duration-300'>{text}</button>
  )
}

export default Button